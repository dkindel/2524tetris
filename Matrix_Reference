Class:
Matrix

Variables:
int[15][10] _matrix		# 15 x 10 int type matrix. int value can be 0, 1 and 2

Methods:
int getValue(int row, int col)				# return the value at the specified location. return -1 if row or col is invalid
bool setValue(int row, int col, int value)	# set the value at the specifed location. return a boolean. false if failed.
int[15][10] getMatrix()					# return the whole matrix
bool setMatrix(int[15][10] newMatrix)	# replace the entire matrix with the new matrix, return false if newMatrix is not valid
void clear2s()							# set all 2's to value 1
